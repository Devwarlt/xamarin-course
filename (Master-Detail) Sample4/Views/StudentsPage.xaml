<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="https://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:local="clr-namspace:Sample3" x:Class="Sample4.Views.StudentsPage">
    <ContentPage.Content>
        <ListView x:Name="studentsList" ItemsSource="{Binding Students}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Label Text="{Binding Name}" HorizontalOptions="Start" />
                        <Label Text="{Binding Shift}" HorizontalOptions="Center" />
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>